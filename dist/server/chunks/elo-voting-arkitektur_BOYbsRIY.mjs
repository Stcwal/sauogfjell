import { c as createComponent } from './astro-component_RdJN0NyS.mjs';
import 'piccolore';
import { r as maybeRenderHead, B as unescapeHTML, x as renderTemplate } from './server_C-z70Qdt.mjs';
import 'clsx';

const html = () => "<h1 id=\"elo-voting--hvordan-det-fungerer\">ELO-voting — hvordan det fungerer</h1>\n<blockquote>\n<p><strong>KOMMENTAR</strong>: Dette er mega-claude, men bare se hvor flink den er til å direkte-oversette fra engelsk :)</p>\n</blockquote>\n<p>Dette dokumentet forklarer ELO-voting-funksjonen fra ende til ende: hvilke filer som\ninneholder hvilke funksjoner, hvordan frontend og backend snakker sammen, og hvorfor\nsystemet er delt opp slik det er.</p>\n<p>For selve ELO-matematikken, se <a href=\"./elo-notater.md\">elo-notater.md</a>. Dette dokumentet\nhandler om <em>rørleggingen</em> rundt den.</p>\n<h2 id=\"overordnet-idé\">Overordnet idé</h2>\n<p>Brukeren får se to sanger og klikker på den de liker best. Dette klikket:</p>\n<ol>\n<li>Registrerer resultatet og oppdaterer ELO-ratingen til begge sanger i databasen.</li>\n<li>Henter et nytt par sanger.</li>\n<li>Bytter inn de nye sangene på siden uten full reload.</li>\n</ol>\n<p>Astro lager statisk HTML som standard, så den eneste måten å oppdatere siden etter et\nklikk uten reload er å kjøre JavaScript i nettleseren som kaller serveren og endrer\nDOM-en. Den nettleser-JS-en er “frontend”; server-endepunktene den kaller er\n“backend”.</p>\n<h2 id=\"filer-og-deres-ansvar\">Filer og deres ansvar</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Fil</th><th>Lag</th><th>Ansvar</th></tr></thead><tbody><tr><td><a href=\"../../../db/config.ts\">db/config.ts</a></td><td>DB-skjema</td><td>Definerer tabellene <code>Songs</code> og <code>Matches</code>.</td></tr><tr><td><a href=\"../../../db/seed.ts\">db/seed.ts</a></td><td>DB-seed</td><td>Setter inn de første sangene med start-ELO og historikk.</td></tr><tr><td><a href=\"../../lib/songs.ts\">src/lib/songs.ts</a></td><td>Backend-logikk</td><td><code>getRandomLowMatchSong()</code> — velger en sang som skal vises. Ren datalogikk, ingen UI.</td></tr><tr><td><a href=\"../api/random-pair.ts\">src/pages/api/random-pair.ts</a></td><td>Backend (API-rute)</td><td><code>GET /api/random-pair</code> — returnerer to sanger som JSON.</td></tr><tr><td><a href=\"../api/vote.ts\">src/pages/api/vote.ts</a></td><td>Backend (API-rute)</td><td><code>POST /api/vote</code> — oppdaterer ELO, historikk, og registrerer kampen.</td></tr><tr><td><a href=\"./index.astro\">src/pages/elo/index.astro</a></td><td>Frontend (side)</td><td>Laster det <em>første</em> paret på server-siden og rendrer komponenten.</td></tr><tr><td><a href=\"./components/EloVoting.astro\">src/pages/elo/components/EloVoting.astro</a></td><td>Frontend (markup + nettleser-JS)</td><td>Rendrer de to kolonnene og håndterer klikk.</td></tr></tbody></table>\n<h2 id=\"hvorfor-denne-oppdelingen\">Hvorfor denne oppdelingen?</h2>\n<ul>\n<li><strong><code>src/lib/</code> inneholder logikk, ikke markup.</strong> <code>getRandomLowMatchSong()</code> er ren\nutvelgingslogikk, så den kan gjenbrukes både av den første server-renderingen\n(<code>index.astro</code>) og av API-ruten (<code>random-pair.ts</code>) uten duplisering.</li>\n<li><strong>API-rutene er broen.</strong> Nettleser-JS kan ikke røre databasen direkte — den kan\nbare gjøre HTTP-forespørsler. Filene i <code>src/pages/api/</code> er server-side endepunkter\nsom <em>kan</em> røre databasen, så de fungerer som den kontrollerte døra mellom nettleser\nog DB.</li>\n<li><strong>Siden rendrer det første paret; API-et serverer alle par etterpå.</strong>\n<code>index.astro</code> kjører allerede på serveren, så den kaller <code>getRandomLowMatchSong()</code>\ndirekte — ingen HTTP-rundtur trengs for den første visningen. Hvert <em>påfølgende</em>\npar kommer fra <code>/api/random-pair</code>, fordi vi da er i nettleseren.</li>\n</ul>\n<h2 id=\"funksjonene\">Funksjonene</h2>\n<h3 id=\"getrandomlowmatchsongnumdistinctvalues-excludesongid--srclibsongsts\"><code>getRandomLowMatchSong(numDistinctValues, excludeSongID?)</code> — <code>src/lib/songs.ts</code></h3>\n<p>Velger én sang som skal vises. For å holde matchene rettferdige favoriserer den\nsanger som har blitt vist færrest ganger: den finner de <code>numDistinctValues</code> laveste\ndistinkte <code>numMatches</code>-verdiene og velger tilfeldig en sang på eller under den\nterskelen.</p>\n<ul>\n<li><code>excludeSongID</code> er valgfri — send med ID-en til den første sangen når du velger den\nandre, slik at en sang ikke kan spille mot seg selv.</li>\n<li>Returnerer <code>null</code> kun hvis det ikke finnes noen kvalifiserte sanger i det hele tatt.</li>\n</ul>\n<h3 id=\"get-apirandom-pair--srcpagesapirandom-pairts\"><code>GET /api/random-pair</code> — <code>src/pages/api/random-pair.ts</code></h3>\n<p>Kaller <code>getRandomLowMatchSong()</code> to ganger (det andre kallet ekskluderer den første\nsangen) og returnerer <code>{ song1, song2 }</code> som JSON. Returnerer HTTP 500 hvis det ikke\nfinnes nok sanger.</p>\n<h3 id=\"post-apivote--srcpagesapivotets\"><code>POST /api/vote</code> — <code>src/pages/api/vote.ts</code></h3>\n<p>Mottar <code>{ winnerId, loserId }</code>. Deretter:</p>\n<ol>\n<li>Laster begge sangene fra DB-en.</li>\n<li><code>calcNewElos(winnerElo, loserElo)</code> beregner de nye ratingene med konstantene <code>K</code>\n(maks endring per kamp) og <code>f</code> (skaleringsfaktor). Vinneren har alltid <code>S = 1</code> og\ntaperen <code>S = 0</code>, siden brukeren valgte vinneren.</li>\n<li>Oppdaterer hver sangs <code>elo</code>, øker <code>numMatches</code>, og legger den nye ratingen til den\nkommaseparerte <code>eloHistory</code>-strengen.</li>\n<li>Setter inn en rad i <code>Matches</code> som registrerer hvem som spilte og hvem som vant.</li>\n</ol>\n<p>Returnerer <code>{ ok: true }</code>, eller HTTP 404 hvis en sang-ID ikke finnes.</p>\n<h3 id=\"nettleser-script--srcpageselocomponentselovotingastro\">Nettleser-script — <code>src/pages/elo/components/EloVoting.astro</code></h3>\n<p>Kjører i nettleseren. Ved last henter den referanser til de to kolonnene, de to\nknappene, og feil-elementet. Hvert knappeklikk kaller <code>vote(winnerId, loserId)</code>, og\nleser ID-ene fra knappenes <code>data-song-id</code>-attributter.</p>\n<p><code>vote()</code>:</p>\n<ol>\n<li>Deaktiverer begge knappene og fjerner eventuell feil (hindrer dobbeltstemming).</li>\n<li><code>POST</code>-er til <code>/api/vote</code>. Kaster feil hvis responsen ikke er OK.</li>\n<li><code>GET</code>-er <code>/api/random-pair</code>. Kaster feil hvis responsen ikke er OK.</li>\n<li>Kaller <code>updateColumn()</code> for hver kolonne for å bytte inn den nye sangens cover,\nnavn, artist og <code>data-song-id</code>.</li>\n<li>Ved en hvilken som helst feil vises en melding og den logges; <code>finally</code> aktiverer\nknappene igjen slik at UI-et aldri låser seg.</li>\n</ol>\n<p><code>updateColumn(col, btn, song)</code> skriver én sangs data inn i DOM-elementene til én\nkolonne.</p>\n<h2 id=\"kommunikasjonsflyt\">Kommunikasjonsflyt</h2>\n<pre class=\"astro-code github-dark\" style=\"background-color:#24292e;color:#e1e4e8; overflow-x: auto;\" tabindex=\"0\" data-language=\"plaintext\"><code><span class=\"line\"><span>                    Første last</span></span>\n<span class=\"line\"><span>  ┌──────────────┐  kaller direkte   ┌────────────────────┐</span></span>\n<span class=\"line\"><span>  │ index.astro  ├──────────────────►│ getRandomLowMatch- │</span></span>\n<span class=\"line\"><span>  │ (server)     │◄──────────────────┤ Song()  (src/lib)  │</span></span>\n<span class=\"line\"><span>  └──────┬───────┘   song1, song2    └─────────┬──────────┘</span></span>\n<span class=\"line\"><span>         │ rendrer                              │ leser</span></span>\n<span class=\"line\"><span>         ▼                                      ▼</span></span>\n<span class=\"line\"><span>  ┌──────────────┐                       ┌────────────┐</span></span>\n<span class=\"line\"><span>  │ EloVoting    │                       │  Database  │</span></span>\n<span class=\"line\"><span>  │ (nettleser)  │                       │  (astro:db)│</span></span>\n<span class=\"line\"><span>  └──────┬───────┘                       └────────────┘</span></span>\n<span class=\"line\"><span>         │                                      ▲</span></span>\n<span class=\"line\"><span>         │ bruker klikker \"Stem\"                │</span></span>\n<span class=\"line\"><span>         │                                      │ oppdaterer / leser</span></span>\n<span class=\"line\"><span>         │  POST /api/vote {winnerId, loserId}  │</span></span>\n<span class=\"line\"><span>         ├─────────────────────────────────────┤</span></span>\n<span class=\"line\"><span>         │  GET  /api/random-pair               │</span></span>\n<span class=\"line\"><span>         │◄──────────────────────────  { song1, song2 }</span></span>\n<span class=\"line\"><span>         │</span></span>\n<span class=\"line\"><span>         ▼ updateColumn() bytter DOM, ingen reload</span></span></code></pre>\n<h2 id=\"datamodell--oppsummering\">Datamodell — oppsummering</h2>\n<ul>\n<li><strong><code>Songs</code></strong> — én rad per sang: <code>songID</code>, <code>artistName</code>, <code>songName</code>, <code>elo</code>,\n<code>numMatches</code>, <code>qualified</code>, <code>albumCoverLink</code>, og <code>eloHistory</code> (kommaseparert\nrating-historikk, f.eks. <code>\"1000,1016,1008\"</code>).</li>\n<li><strong><code>Matches</code></strong> — én rad per stemme: <code>matchID</code>, <code>songID1</code>, <code>songID2</code>,\n<code>songIDwinner</code>. Dette er den permanente loggen over hver stemme, atskilt fra den\nper-sang <code>eloHistory</code>-strengen.</li>\n</ul>\n<p>De to lagringene overlapper med vilje: <code>Matches</code> er den relasjonelle kilden til\nsannhet for <em>hva som skjedde</em>, mens <code>eloHistory</code> er en denormalisert, klar-til-å-\nplotte rating-tidslinje for hver sang.</p>";

				const frontmatter = {};
				const file = "/Users/stian/documents/data/github/private/sauogfjell/src/pages/elo/elo-voting-arkitektur.md";
				const url = "/elo/elo-voting-arkitektur";
				function rawContent() {
					return "# ELO-voting — hvordan det fungerer\n>**KOMMENTAR**: Dette er mega-claude, men bare se hvor flink den er til å direkte-oversette fra engelsk :)\n\nDette dokumentet forklarer ELO-voting-funksjonen fra ende til ende: hvilke filer som\ninneholder hvilke funksjoner, hvordan frontend og backend snakker sammen, og hvorfor\nsystemet er delt opp slik det er.\n\nFor selve ELO-matematikken, se [elo-notater.md](./elo-notater.md). Dette dokumentet\nhandler om *rørleggingen* rundt den.\n\n## Overordnet idé\n\nBrukeren får se to sanger og klikker på den de liker best. Dette klikket:\n\n1. Registrerer resultatet og oppdaterer ELO-ratingen til begge sanger i databasen.\n2. Henter et nytt par sanger.\n3. Bytter inn de nye sangene på siden uten full reload.\n\nAstro lager statisk HTML som standard, så den eneste måten å oppdatere siden etter et\nklikk uten reload er å kjøre JavaScript i nettleseren som kaller serveren og endrer\nDOM-en. Den nettleser-JS-en er \"frontend\"; server-endepunktene den kaller er\n\"backend\".\n\n## Filer og deres ansvar\n\n| Fil | Lag | Ansvar |\n| --- | --- | --- |\n| [db/config.ts](../../../db/config.ts) | DB-skjema | Definerer tabellene `Songs` og `Matches`. |\n| [db/seed.ts](../../../db/seed.ts) | DB-seed | Setter inn de første sangene med start-ELO og historikk. |\n| [src/lib/songs.ts](../../lib/songs.ts) | Backend-logikk | `getRandomLowMatchSong()` — velger en sang som skal vises. Ren datalogikk, ingen UI. |\n| [src/pages/api/random-pair.ts](../api/random-pair.ts) | Backend (API-rute) | `GET /api/random-pair` — returnerer to sanger som JSON. |\n| [src/pages/api/vote.ts](../api/vote.ts) | Backend (API-rute) | `POST /api/vote` — oppdaterer ELO, historikk, og registrerer kampen. |\n| [src/pages/elo/index.astro](./index.astro) | Frontend (side) | Laster det *første* paret på server-siden og rendrer komponenten. |\n| [src/pages/elo/components/EloVoting.astro](./components/EloVoting.astro) | Frontend (markup + nettleser-JS) | Rendrer de to kolonnene og håndterer klikk. |\n\n## Hvorfor denne oppdelingen?\n\n- **`src/lib/` inneholder logikk, ikke markup.** `getRandomLowMatchSong()` er ren\n  utvelgingslogikk, så den kan gjenbrukes både av den første server-renderingen\n  (`index.astro`) og av API-ruten (`random-pair.ts`) uten duplisering.\n- **API-rutene er broen.** Nettleser-JS kan ikke røre databasen direkte — den kan\n  bare gjøre HTTP-forespørsler. Filene i `src/pages/api/` er server-side endepunkter\n  som *kan* røre databasen, så de fungerer som den kontrollerte døra mellom nettleser\n  og DB.\n- **Siden rendrer det første paret; API-et serverer alle par etterpå.**\n  `index.astro` kjører allerede på serveren, så den kaller `getRandomLowMatchSong()`\n  direkte — ingen HTTP-rundtur trengs for den første visningen. Hvert *påfølgende*\n  par kommer fra `/api/random-pair`, fordi vi da er i nettleseren.\n\n## Funksjonene\n\n### `getRandomLowMatchSong(numDistinctValues, excludeSongID?)` — `src/lib/songs.ts`\n\nVelger én sang som skal vises. For å holde matchene rettferdige favoriserer den\nsanger som har blitt vist færrest ganger: den finner de `numDistinctValues` laveste\ndistinkte `numMatches`-verdiene og velger tilfeldig en sang på eller under den\nterskelen.\n\n- `excludeSongID` er valgfri — send med ID-en til den første sangen når du velger den\n  andre, slik at en sang ikke kan spille mot seg selv.\n- Returnerer `null` kun hvis det ikke finnes noen kvalifiserte sanger i det hele tatt.\n\n### `GET /api/random-pair` — `src/pages/api/random-pair.ts`\n\nKaller `getRandomLowMatchSong()` to ganger (det andre kallet ekskluderer den første\nsangen) og returnerer `{ song1, song2 }` som JSON. Returnerer HTTP 500 hvis det ikke\nfinnes nok sanger.\n\n### `POST /api/vote` — `src/pages/api/vote.ts`\n\nMottar `{ winnerId, loserId }`. Deretter:\n\n1. Laster begge sangene fra DB-en.\n2. `calcNewElos(winnerElo, loserElo)` beregner de nye ratingene med konstantene `K`\n   (maks endring per kamp) og `f` (skaleringsfaktor). Vinneren har alltid `S = 1` og\n   taperen `S = 0`, siden brukeren valgte vinneren.\n3. Oppdaterer hver sangs `elo`, øker `numMatches`, og legger den nye ratingen til den\n   kommaseparerte `eloHistory`-strengen.\n4. Setter inn en rad i `Matches` som registrerer hvem som spilte og hvem som vant.\n\nReturnerer `{ ok: true }`, eller HTTP 404 hvis en sang-ID ikke finnes.\n\n### Nettleser-script — `src/pages/elo/components/EloVoting.astro`\n\nKjører i nettleseren. Ved last henter den referanser til de to kolonnene, de to\nknappene, og feil-elementet. Hvert knappeklikk kaller `vote(winnerId, loserId)`, og\nleser ID-ene fra knappenes `data-song-id`-attributter.\n\n`vote()`:\n\n1. Deaktiverer begge knappene og fjerner eventuell feil (hindrer dobbeltstemming).\n2. `POST`-er til `/api/vote`. Kaster feil hvis responsen ikke er OK.\n3. `GET`-er `/api/random-pair`. Kaster feil hvis responsen ikke er OK.\n4. Kaller `updateColumn()` for hver kolonne for å bytte inn den nye sangens cover,\n   navn, artist og `data-song-id`.\n5. Ved en hvilken som helst feil vises en melding og den logges; `finally` aktiverer\n   knappene igjen slik at UI-et aldri låser seg.\n\n`updateColumn(col, btn, song)` skriver én sangs data inn i DOM-elementene til én\nkolonne.\n\n## Kommunikasjonsflyt\n\n```\n                    Første last\n  ┌──────────────┐  kaller direkte   ┌────────────────────┐\n  │ index.astro  ├──────────────────►│ getRandomLowMatch- │\n  │ (server)     │◄──────────────────┤ Song()  (src/lib)  │\n  └──────┬───────┘   song1, song2    └─────────┬──────────┘\n         │ rendrer                              │ leser\n         ▼                                      ▼\n  ┌──────────────┐                       ┌────────────┐\n  │ EloVoting    │                       │  Database  │\n  │ (nettleser)  │                       │  (astro:db)│\n  └──────┬───────┘                       └────────────┘\n         │                                      ▲\n         │ bruker klikker \"Stem\"                │\n         │                                      │ oppdaterer / leser\n         │  POST /api/vote {winnerId, loserId}  │\n         ├─────────────────────────────────────┤\n         │  GET  /api/random-pair               │\n         │◄──────────────────────────  { song1, song2 }\n         │\n         ▼ updateColumn() bytter DOM, ingen reload\n```\n\n## Datamodell — oppsummering\n\n- **`Songs`** — én rad per sang: `songID`, `artistName`, `songName`, `elo`,\n  `numMatches`, `qualified`, `albumCoverLink`, og `eloHistory` (kommaseparert\n  rating-historikk, f.eks. `\"1000,1016,1008\"`).\n- **`Matches`** — én rad per stemme: `matchID`, `songID1`, `songID2`,\n  `songIDwinner`. Dette er den permanente loggen over hver stemme, atskilt fra den\n  per-sang `eloHistory`-strengen.\n\nDe to lagringene overlapper med vilje: `Matches` er den relasjonelle kilden til\nsannhet for *hva som skjedde*, mens `eloHistory` er en denormalisert, klar-til-å-\nplotte rating-tidslinje for hver sang.\n";
				}
				async function compiledContent() {
					return await html();
				}
				function getHeadings() {
					return [{"depth":1,"slug":"elo-voting--hvordan-det-fungerer","text":"ELO-voting — hvordan det fungerer"},{"depth":2,"slug":"overordnet-idé","text":"Overordnet idé"},{"depth":2,"slug":"filer-og-deres-ansvar","text":"Filer og deres ansvar"},{"depth":2,"slug":"hvorfor-denne-oppdelingen","text":"Hvorfor denne oppdelingen?"},{"depth":2,"slug":"funksjonene","text":"Funksjonene"},{"depth":3,"slug":"getrandomlowmatchsongnumdistinctvalues-excludesongid--srclibsongsts","text":"getRandomLowMatchSong(numDistinctValues, excludeSongID?) — src/lib/songs.ts"},{"depth":3,"slug":"get-apirandom-pair--srcpagesapirandom-pairts","text":"GET /api/random-pair — src/pages/api/random-pair.ts"},{"depth":3,"slug":"post-apivote--srcpagesapivotets","text":"POST /api/vote — src/pages/api/vote.ts"},{"depth":3,"slug":"nettleser-script--srcpageselocomponentselovotingastro","text":"Nettleser-script — src/pages/elo/components/EloVoting.astro"},{"depth":2,"slug":"kommunikasjonsflyt","text":"Kommunikasjonsflyt"},{"depth":2,"slug":"datamodell--oppsummering","text":"Datamodell — oppsummering"}];
				}

				const Content = createComponent((result, _props, slots) => {
					const { layout, ...content } = frontmatter;
					content.file = file;
					content.url = url;

					return renderTemplate`<meta charset="utf-8">${maybeRenderHead()}${unescapeHTML(html())}`;
				});

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
	__proto__: null,
	Content,
	compiledContent,
	default: Content,
	file,
	frontmatter,
	getHeadings,
	rawContent,
	url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
