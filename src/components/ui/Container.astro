---
---

<section class="container">
    <slot />
</section>

<style>
    .container {
        /* page-container dimension, not spacing — same category as --header-height */
        max-width: 1000px;
        margin: 0 auto;
        padding: var(--space-4);
        display: flex;
        flex-direction: column;
        gap: var(--space-4);
    }
</style>
